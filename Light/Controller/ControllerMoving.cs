using Light.Entities;
using Light.Map;
using System;
using System.Drawing;

namespace Light.Controller
{
    static class ControllerMoving
    {
        public static bool ObjectIdentification(Player player, Point directon)
        {
            //if ((player.directionX + player.positionX + ControllerCamera.cameraOffset.X) / CreatingMap.sizeMap == )
            //    player.speedMoving = 0;

            return false;
        }
    }
}
